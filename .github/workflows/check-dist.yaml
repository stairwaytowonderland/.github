# In TypeScript actions, `dist/` is a special directory. When you reference
# an action with the `uses:` property, `dist/index.js` is the code that will be
# run. For this project, the `dist/index.js` file is transpiled from other
# source files. This workflow ensures the `dist/` directory contains the
# expected transpiled code.
#
# If this workflow is run from a feature branch, it will act as an additional CI
# check and fail if the checked-in `dist/` directory does not match what is
# expected from the build.
name: Check Transpiled JavaScript

on:
  workflow_call:
    inputs:
      build-command:
        description: 'The build command to run.'
        type: string
        default: |
          npm run bundle
      dist-dir:
        description: 'The dist/ directory to check.'
        type: string
        default: 'dist'
      node-cache-key:
        description: 'The Node.js cache key to use.'
        type: string
        default: 'npm'
      node-version-file:
        description: 'The Node.js version file to use.'
        type: string
        default: '.node-version'

permissions:
  contents: read

jobs:
  check-dist:
    name: Check dist/
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        id: setup-node
        uses: actions/setup-node@v7
        with:
          node-version-file: ${{ inputs.node-version-file }}
          cache: ${{ inputs.node-cache-key }}

      - name: Remove dist/ Directory
        id: remove-dist
        env:
          DIST_DIR: ${{ inputs.dist-dir }}
        run: npx rimraf ${DIST_DIR}

      - name: Install Dependencies
        id: install
        run: npm ci

      - name: Build dist/ Directory
        id: build
        run: ${{ inputs.build-command }}

      # This will fail the workflow if the `dist/` directory is different than
      # expected.
      - name: Compare Directories
        id: diff
        env:
          DIST_DIR: ${{ inputs.dist-dir }}
        run: |
          if [ ! -d "${DIST_DIR}" ]; then
            echo "Expected ${DIST_DIR} directory does not exist.  See status below:"
            ls -la ./
            exit 1
          fi
          if [ "$(git diff --ignore-space-at-eol --text "${DIST_DIR}" | wc -l)" -gt "0" ]; then
            echo "Detected uncommitted changes after build. See status below:"
            git diff --ignore-space-at-eol --text "${DIST_DIR}"
            exit 1
          fi

      # If `dist/` was different than expected, upload the expected version as a
      # workflow artifact.
      - if: ${{ failure() && steps.diff.outcome == 'failure' }}
        name: Upload Artifact
        id: upload
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: ${{ inputs.dist-dir }}/
